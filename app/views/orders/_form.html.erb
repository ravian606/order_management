<% content_for :head do %>  
  <script type="text/javascript">
    // $(document).ready(function() { 
    //   $("#prod_dd").select2();
    //   $("#order_product_id").select2();
    // });
  </script>
<% end %>

<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
        <% order.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row mb-4">
    <%= form.label :site_id, class: 'col-sm-2 col-form-label' %>
    <div class='col-sm-10'>
      <%= form.select :site_id, options_for_select(sites.map{|site| [site.name, site.id]}), { prompt: 'Select Site' }, { class: "form-control", id: 'site_dd' } %>
    </div>
  </div>

  <h2>Add Products</h2>
  <%= form.fields_for :product_order_details do |product_order_details_form| %>
    <%= render "product_order_details_fields", f: product_order_details_form, products: products %>
  <% end %>

  <div class="actions">
    <%= form.submit class: 'btn btn-primary' %>
  </div>
<% end %>